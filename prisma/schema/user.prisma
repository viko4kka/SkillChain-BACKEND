model User {
    id          Int        @id @default(autoincrement())
    linkedinId  String?  @unique
    firstName   String
    lastName    String
    description String?
    gitUrl      String?
    linkedinUrl String?
    languages   Language[]
}
