model User {
    id          Int        @id @default(autoincrement())
    linkedinId  String?    @unique
    firstName   String
    lastName    String
    description String?
    job         String?
    gitUrl      String?
    linkedinUrl String?
    languages   Language[]
    projects    Project[]
}
