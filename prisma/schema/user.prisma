model User {
    id          Int        @id @default(autoincrement())
    firstName   String
    lastName    String
    description String?
    gitUrl      String?
    linkedinUrl String?
    languages   Language[]
    projects    Project[]
}