model UserSkill {
  skillId     Int    
  skillName   String
  description String?
  userId Int
  user   User   @relation(fields: [userId], references: [id])
  skill  Skill  @relation(fields: [skillId], references: [id])
  @@id([userId, skillId])
}