model Project {
    id          Int     @id @default(autoincrement())
    idUser      Int
    projectName String
    description String
    githubLink  String?
    websiteLink String?
    
    startDate   DateTime
    endDate     DateTime?
    user User @relation(fields: [idUser], references: [id])
}